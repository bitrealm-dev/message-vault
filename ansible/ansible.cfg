[defaults]
inventory = inventory/hosts
roles_path = roles
host_key_checking = True
retry_files_enabled = False
stdout_callback = default
# Prefer Python 3 on Ubuntu 26.04 targets
interpreter_python = auto_silent

[privilege_escalation]
become = True
become_method = sudo
