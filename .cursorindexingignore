# Skip unless the task is about this tree.
# These stay in git and Agent can still open a file, but they are left
# out of codebase search so they do not flood context.
#
# Agent Grep and Glob still traverse these paths on a repo-wide search.
# Move a path into .cursorignore only if it should be a hard block.

# Restored Next.js browse UI. Product UI is web/.
web-next/

# Legacy Slint desktop GUI. Product desktop app is src-tauri/ + web/.
crates/message-vault-io-gui/

# Vendored sqlx-sqlite fork. Open only when changing that driver.
vendor/
