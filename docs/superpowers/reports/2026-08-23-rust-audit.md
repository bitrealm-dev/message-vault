# Product Rust audit — 2026-08-23

Scope: workspace crates except `crates/message-vault-io-gui`, plus `src-tauri`. Read-only; no code changed.

## Summary

| Severity | Count |
|---|---|
| high | 3 |
| medium | 30 |
| low | 32 |
| **Total** | **65** |

| Category | Findings |
|---|---|
| duplication | 20 |
| docs-coverage | 14 |
| docs-quality | 8 |
| api-design | 6 |
| error-handling | 5 |
| structure | 5 |
| best-practices | 3 |
| http-docs | 2 |
| unsafe | 2 |
| **Total** | **65** |

## Mechanical signals

Commands and counts anyone can rerun.

- **Clippy**: `cargo clippy --workspace` and `cargo clippy --manifest-path src-tauri/Cargo.toml` — 0 warnings in the workspace and in src-tauri.
- **Missing-docs heuristic**: `find crates src-tauri/src -name '*.rs'` (excluding target/, `message-vault-io-gui`, `main.rs`, `lib.rs`) piped through an awk rule that flags `pub fn/struct/enum/trait/const/type/mod` lines whose previous non-blank line is not a `///` comment — 328 flagged lines. The rule also fires when an attribute (e.g. `#[derive]`) sits between the doc comment and the item, so the count includes that false-positive class.
- **Unsafe sites**: `grep -rn "unsafe" crates src-tauri/src --include='*.rs'` — 13 hits: 7 real code sites (unsafe blocks in asset_uploads.rs:164/560/581, media/tools.rs:407/412, ffmpeg.rs:65/79) and 6 string/doc mentions (`bail!` messages and test assertions for "unsafe attachment path", one doc line).
- **Largest files**: `find crates src-tauri/src -name '*.rs' -exec wc -l {} + | sort -rn | head -40` — top file is `crates/vault/server/src/server.rs` at 3,452 lines.
- **Utoipa extraction**: `python3 .tmp/rust-audit/extract-openapi.py docs/src/assets/openapi.json` exactly 50 operations — 32 NO-SUMMARY, 6 ECHOED-ROUTE-SUMMARY, 7 ERRORS-SECTION-IN-DESCRIPTION, 43 NO-DESCRIPTION.

The raw evidence files under .tmp/rust-audit/ are gitignored and ephemeral; the counts above are what matter.

## Findings by category

Categorized findings, ordered by severity within each category.

## duplication — 20 findings

### medium

- **[medium] bump / prepare_conversation / pending_to_document triads duplicated in 5 exporters** — `crates/exporters/go-sms-pro-exporter/src/emit.rs:55`. **Evidence:** `fn bump(report, key, by)` is byte-identical at go-sms-pro:55, openextract:27, whatsapp:31, imazing:29, sms-backup-plus:31. `prepare_conversation` has the same signature at 444/212/483/630/242 with near-identical bodies; pending_to_document at 531/369/520/721/336 exists but signatures and bodies differ. **Suggestion:** Hoist these three helpers into message-vault-io-core (or message-ir) and have each exporter call the shared versions.
- **[medium] Content-addressed attachment naming and skip-if-exists copy duplicated in 4 exporters** — `crates/exporters/go-sms-pro-exporter/src/emit.rs:239`. **Evidence:** `digest_hex[..16.min(digest_hex.len())]` appears at go-sms-pro/emit.rs:239, imazing/attachments.rs:244, imessage-ir/emit.rs:454, sms-backup-plus/assets.rs:154. `{local-date}-{digest16}{ext}` naming plus idempotent copy-if-missing is verified in imessage-ir and imazing. **Suggestion:** Extract a shared attachment_dest_name and copy-if-missing helper in message-vault-io-core used by all four exporters.
- **[medium] Attachment metadata shape triplicated across libs** — `crates/libs/ir/src/lib.rs:285`. **Evidence:** IrAttachment (ir:285) carries path, original_name, mime_type, digest_sha256, is_sticker, transcription, sticker_effect; csv::AttachmentCell (csv/src/lib.rs:15) carries the same core set; mail::MailAttachment (mail/src/lib.rs:59) carries the same set plus bytes; ir-format write.rs:258-270 maps field-by-field by hand, and readers mirror it. **Suggestion:** Define one shared attachment-metadata struct in message-ir and reuse it in the csv cell and mail MIME layers instead of three copies plus manual converters.
- **[medium] thread_tags_api.rs and contact_groups_api.rs are near-identical CRUD clones** — `crates/vault/server/src/thread_tags_api.rs:35`. **Evidence:** TagError (thread_tags_api.rs:35-40) and GroupError (contact_groups_api.rs:41-46) share the same 4 variants and identical From<rusqlite::Error>; is_reserved_tag_name/is_reserved_group_name, reserved_*_error, normalize_new_name (byte-identical bodies), list_tags/list_groups (identical except table names) all mirror each other. **Suggestion:** Factor the shared named-membership pattern (reserved names, normalization, list/rename/delete/membership over a table) into one generic helper used by both modules.
- **[medium] Cancel-flag reset, thread spawn, and extract:* event routing boilerplate duplicated across all four job commands** — `src-tauri/src/commands/extract.rs:179`. **Evidence:** Same scaffolding in extract.rs:181-191/205-249, format.rs:47-55/59-95, pull.rs:42-50/54-108, push.rs:93-201: two state.lock() round-trips (reset then clone cancel), thread::spawn, then emit extract:log / extract:finished / extract:error with the same summary-fallback (last_log_line_or) and error-detail ({err:#}) patterns. **Suggestion:** Add a shared helper in commands/mod.rs (e.g. spawn_background_job(state, app, run)) that resets and clones the cancel flag, spawns the thread, and routes a Result<RunResult, anyhow::Error> to the extract:* events.
- **[medium] convert_export output-overlap preamble duplicated in 4 exporters** — `crates/exporters/go-sms-pro-exporter/src/emit.rs:754`. **Evidence:** create_dir_all + canonicalize + `bail!("output {} must not be the same as, or contain, the input {}")` block confirmed at go-sms-pro emit.rs:754-769, imazing-exporter emit.rs:97-109, openextract-exporter emit.rs:69-83, sms-backup-plus-exporter emit.rs:735-753 (sms-backup-plus loops over multiple inputs but identical pattern). **Suggestion:** Add an `ExporterConfig::prepare_outputs(input, output)` helper (create_dir_all, canonicalize, overlap bail) to message-vault-io-core next to `ensure_output_dir` and call it from all four convert_export functions.
- **[medium] run() pipeline skeleton duplicated across 5 exporters** — `crates/exporters/go-sms-pro-exporter/src/run.rs:19`. **Evidence:** Confirmed: go-sms-pro run.rs:19-43, imazing run.rs:19-61, openextract run.rs:19-43, sms-backup-restore run.rs:19-43 share check_cancel/require_input/contacts_csv_vcf + resolve_contacts_cli/ExportTransforms::from_configs + transforms.log/media-failed-for-all bail/sink.log_lines/report.summary_lines/RunResult nearly line-for-line; whatsapp run.rs shares the transform+bail+log_lines+summary_lines tail. **Suggestion:** Extract a shared run_pipeline helper in message-vault-io-core that takes a convert closure and handles contacts resolution, transforms, media-failure bail, and summary so each exporter's run() keeps only source-specific conversion.
- **[medium] main() CLI driver body duplicated in 6 of 7 exporter binaries** — `crates/exporters/go-sms-pro-exporter/src/main.rs:13`. **Evidence:** Confirmed the parse_date_range + OutputFormat::parse + compress_options_from_cli + ExporterConfig{...} + run + print_result sequence in go-sms-pro, imazing, openextract, sms-backup-restore, whatsapp, sms-backup-plus main.rs (imessage-ir diverges); only source-specific args differ. **Suggestion:** Add a shared `run_cli` driver in message-vault-io-core (CommonCli already lives there) that takes a SourceConfig builder closure and performs date/format/media parsing plus print_result.
- **[medium] JSONL journal module implemented twice (vault-push and vault-pull)** — `crates/cli/vault-push/src/journal.rs:131`. **Evidence:** Confirmed: vault-pull/src/journal.rs:59-155 reimplements load (open-iterate-skip-empty-parse-JSONL) at 59-94, append at 102-115, compact (sort + tmp-file + rename) at 122-155, mirroring vault-push/src/journal.rs:131-301; only the event enum differs. **Suggestion:** Extract one generic JSONL journal crate (load/append/compact parameterized over the event type) into crates/libs and have both vault-push and vault-pull depend on it.
- **[medium] convert_smoke test scaffolding duplicated across exporters** — `crates/exporters/go-sms-pro-exporter/tests/convert_smoke.rs:36`. **Evidence:** Confirmed: convert() wrapper + empty_contacts() identical at go-sms-pro tests/convert_smoke.rs:11-33 and sms-backup-restore:11-35; csv-count-with-.meta.json-filter and header-assert blocks duplicated (sms-backup-restore:59-100, sms-backup-plus:102-116). **Suggestion:** Publish a shared test-support module (e.g. message_vault_io_core::testutil with assert_csv_output/empty_contacts) behind a test feature and reuse it in every convert_smoke.rs.

### low

- **[low] ConversationDocument test fixtures duplicated across crates** — `crates/libs/ir-format/src/format_sink.rs:195`. **Evidence:** Confirmed: fn tiny_doc(text) at format_sink.rs:195, fn sample_doc() at ir-format/src/lib_tests.rs:14, and fn sample_doc() at reexport/src/lib.rs:398 are near-identical hand-built ConversationDocuments with the same chat_identifier/participant/timestamp shapes. **Suggestion:** Share one test-fixture builder (e.g. a #[cfg(test)] helper in message-ir or a dev-dependency fixture crate) instead of three copies.
- **[low] clap_command() and its test duplicated across all 7 exporter cli.rs files** — `crates/exporters/go-sms-pro-exporter/src/cli.rs:27`. **Evidence:** Verified: `pub fn clap_command() -> Command { Cli::command() }` plus the `clap_command_uses_binary_name` test module appears in all seven exporter cli.rs files (go-sms-pro:27, openextract:22, whatsapp:72, imazing:26, sms-backup-plus:56, sms-backup-restore:27, imessage-ir:46); bodies identical except the binary-name string asserted in each test. **Suggestion:** Move the shared clap_command scaffolding into message-vault-io-core's feature-gated `cli` module and re-export per crate.
- **[low] truncate() (and HttpSession::new boilerplate) duplicated verbatim across the two CLI crates** — `crates/cli/vault-push/src/http.rs:174`. **Evidence:** crates/cli/vault-push/src/http.rs:174-180 and crates/cli/vault-pull/src/http.rs:395-401 are byte-identical (same doc line, same ellipsis format); the "Blocking HTTP client with a connection pool" doc and Client::builder().pool_max_idle_per_host(16) block repeat too (push:131/138, pull:193/200). **Suggestion:** Move truncate (and ideally the shared HttpSession constructor) into message-vault-io-core or a small cli-shared helper crate used by both vault-push and vault-pull.
- **[low] Pagination limit constants duplicated across contacts, conversations, and export modules** — `crates/vault/server/src/contacts_api.rs:16`. **Evidence:** DEFAULT_LIST_LIMIT/MAX_LIST_LIMIT/MAX_LIST_OFFSET declared at contacts_api.rs:16-19 and conversations_api.rs:12-15 with the same doc text "Cap expensive OFFSET skips on ... list pages"; export_api.rs:12-15 has its own DEFAULT_EXPORT_LIMIT/MAX_EXPORT_LIMIT/MAX_EXPORT_OFFSET. **Suggestion:** Define page-limit constants once (e.g. in a shared server module) and reference them from all three.
- **[low] bump() report-extra counter helper identical in 5 exporters** — `crates/exporters/go-sms-pro-exporter/src/emit.rs:55`. **Evidence:** Confirmed: `fn bump(report, key, by) { *report.extra.entry(key.to_string()).or_insert(0) += by; }` byte-identical at go-sms-pro emit.rs:55-57, imazing emit.rs:29-31, openextract emit.rs:27-29, sms-backup-plus emit.rs:31-33, whatsapp emit.rs:31-33. **Suggestion:** Add `ExportReport::bump(key, by)` to message-vault-io-core (which owns ExportReport) and delete the five private copies.
- **[low] 64KB-chunk SHA-256 file hasher written three times** — `crates/exporters/whatsapp-exporter/src/emit.rs:440`. **Evidence:** Confirmed: file_sha256 at whatsapp emit.rs:440, stream_sha256 at imazing attachments.rs:266, hash_file_sha256 at ir-format export_transforms.rs:203 — same 64KB [0u8; 64*1024] buffer loop with Sha256::update and hex finish. **Suggestion:** Implement one media::file_sha256 in libs/media (or message-vault-io-core) and reuse it in whatsapp, imazing, and ir-format.
- **[low] CSV col()/field() header helpers byte-identical in two CSV exporters** — `crates/exporters/openextract-exporter/src/parse.rs:140`. **Evidence:** Confirmed: col()/field() byte-identical at openextract parse.rs:140-149 and imazing parse.rs:216-225, while crates/libs/csv exists and is used by both exporters. **Suggestion:** Move col()/field() into libs/csv and import them from both exporters.
- **[low] truncate() and HttpSession::new() duplicated between vault-push and vault-pull** — `crates/cli/vault-push/src/http.rs:174`. **Evidence:** Confirmed: truncate() byte-identical at vault-push http.rs:174-180 and vault-pull http.rs:395-401; HttpSession::new with pool_max_idle_per_host(16) identical at vault-push http.rs:136-142 and vault-pull http.rs:198-204; vault-pull Cargo.toml:28 already depends on vault-push. **Suggestion:** Share one vault HTTP client module (vault-pull already depends on vault_push::authenticate) and re-export truncate from it.
- **[low] clap_command() + clap_command_uses_binary_name test copy-pasted in all 7 exporters** — `crates/exporters/go-sms-pro-exporter/src/cli.rs:34`. **Evidence:** Confirmed: `pub fn clap_command() -> Command { Cli::command() }` plus the clap_command_uses_binary_name test present in all seven cli.rs files (only the asserted binary name differs); `pub use cli::clap_command` in 7 lib.rs files and the convert_smoke module hookup in 6. **Suggestion:** Provide the pattern once via a #[macro_export] test macro (or a shared clap-command factory in message-vault-io-core) and delete the seven copy-pasted modules.
- **[low] mime_for_ext maps partially duplicated between exporters** — `crates/exporters/go-sms-pro-exporter/src/emit.rs:76`. **Evidence:** Confirmed: six of seven arms (.jpg/.jpeg/.png/.gif/.3gp/.mp4/.amr) shared with sms-backup-plus-exporter/src/assets.rs:84-97; only .webp/.mp3/.m4a extra arms differ (go-sms-pro additionally has .wav). **Suggestion:** Hoist a shared mime-for-extension table (e.g. libs/media::mime_for_ext) and have each exporter extend only its source-specific extras.

---
## docs-coverage — 14 findings

### high

- **[high] Core IR model types have no doc comments** — `crates/libs/ir/src/lib.rs:20`. **Evidence:** Confirmed: ConversationDocument at lib.rs:20 has no doc comment (line 19 is the derive); ExportMeta:31, IrConversationType:42, HandleType:65, ConversationMeta:93, ConversationStats:102, IrParticipant:110, IrService:119, IrMessage:253, IrDirection:270, IrAttachment:285, ConversationHeader:452 all bare. Only a handful of fields (packaging_stem_suffix, owner_display_name, size_bytes, missing_reason, bytes) are documented. **Suggestion:** Add a doc comment to every pub type, variant, and field in message-ir — the model every exporter writes — describing what each serialized field means.

### medium

- **[medium] Re-exported sbr reader types undocumented** — `crates/libs/sbr/src/read.rs:71`. **Evidence:** Confirmed: pub struct Record at read.rs:71 has no doc comment; ConversationKind:24, AttachmentBlob:49, SourceFields:59, ParseStats:91 are also bare; all five are re-exported at sbr/src/lib.rs:8-11. Record's fields chat_key, participant_digits, date_ms, contact_name are undocumented. **Suggestion:** Document Record and its fields (chat_key, participant_digits, date_ms, contact_name) plus the other four re-exported reader types.
- **[medium] Re-exported ffmpeg probe API undocumented** — `crates/libs/media/src/tools.rs:63`. **Evidence:** Confirmed: pub struct FfmpegToolsProbe at tools.rs:63 has no doc comment; pub fn ffmpeg_available() at tools.rs:70 and pub fn probe_ffmpeg_tools() at tools.rs:141 are bare; all three re-exported at media/src/lib.rs:18. **Suggestion:** Add doc comments to FfmpegToolsProbe, its fields, ffmpeg_available, and probe_ffmpeg_tools (and MediaReport at process.rs:11).
- **[medium] ValidateMode/ValidateReport/VcfCard lack docs** — `crates/libs/contacts/src/validate.rs:13`. **Evidence:** Confirmed: pub enum ValidateMode at validate.rs:13 and pub struct ValidateReport at validate.rs:22 have no doc comments (variants/fields documented); pub struct VcfCard at vcf.rs:8 has none and its fn_raw/n_family/n_given/n_middle fields are bare; all re-exported from contacts lib.rs:23-26,31. **Suggestion:** Add one-line docs to ValidateMode, ValidateReport, and VcfCard (the latter needs field-level docs for fn_raw/n_family/n_given/n_middle).
- **[medium] ExporterConfig pub fields are undocumented** — `crates/core/message-vault-io-core/src/config.rs:104`. **Evidence:** Verified at config.rs:101-119: output, date_range, contacts, obfuscate, cancel, source are bare pub fields on the struct shared by 7 exporters and the GUI. Only timezone, media, and output_format of 11 fields carry docs; 6 core fields are bare. **Suggestion:** Document each ExporterConfig field, pointing CLI readers at the same-named CommonCli flag.
- **[medium] Form struct: only 1 of 32 pub fields documented** — `crates/core/message-vault-io-core/src/exporters.rs:354`. **Evidence:** Verified: `pub struct Form` (exporters.rs:354) exposes 32 pub fields consumed by the GUI form; only `output_format` (line 373) carries a doc comment; input, output, contacts, attachment_root, backup_password and the rest are bare. **Suggestion:** Add a doc comment per Form field, or a struct-level field table covering all 32.
- **[medium] dump-cli-docs is a library crate with a fully undocumented public API** — `crates/cli/dump-cli-docs/src/lib.rs:3`. **Evidence:** pub struct PageSpec (line 3), PAGE_SPECS (line 10), render_page, command_for, page_markdown, write_pages (lines 79-123) all lack doc comments; file has no //! module intro and the crate has no #![warn(missing_docs)]. **Suggestion:** Add a //! module intro and one-line doc comments (with # Errors sections for the fallible fns) to every pub item, or make the crate a binary-only tool.
- **[medium] No #![warn(missing_docs)] in a library crate with many undocumented pub items** — `crates/vault/server/src/lib.rs:1`. **Evidence:** lib.rs is only a one-line //! intro (line 1) with no missing_docs lint; .tmp/rust-audit/missing-docs-heuristic.txt lists 178 server-crate entries including the cited ApiTokenItem, AssetStats, StoredAsset, is_demo_account, generate_session_token, ExportPageOpts. **Suggestion:** Add #![warn(missing_docs)] to lib.rs and document the public surface, or shrink the pub surface (see lib.rs pub mod finding).
- **[medium] Modules without //! intros, including the 3039-line import.rs and 1028-line assets.rs** — `crates/vault/server/src/import.rs:1`. **Evidence:** import.rs:1 is 'use std::collections::HashMap;' with no module doc; assets.rs:1, jsonl.rs:1, config.rs:1, db/account_profile.rs:1, db/contacts.rs:1, db/schema.rs:1 all begin with use statements and no //! intro, while sibling modules (e.g. thread_tags_api.rs:1, contact_groups_api.rs:1, conversations_api.rs:1) do have intros. **Suggestion:** Add a //! intro to import.rs and assets.rs (the two largest undocumented modules) describing responsibilities, and to the remaining five small ones.
- **[medium] utoipa::ToSchema response structs without doc comments get schema descriptions of empty string** — `crates/vault/server/src/contacts_api.rs:22`. **Evidence:** pub struct ContactListPage (contacts_api.rs:21-22) has no doc comment; likewise ConversationListPage (conversations_api.rs:17-18), ApiTokenItem (api_tokens_api.rs:15-16), StoredAsset (assets.rs:15-16), ExportMessagesResponse (export_api.rs:33-34) — all #[derive(utoipa::ToSchema)] with no rustdoc. **Suggestion:** Add one-line docs to each ToSchema struct so generated OpenAPI components carry descriptions.

### low

- **[low] imazing emit.rs private helpers lack doc comments** — `crates/exporters/imazing-exporter/src/emit.rs:422`. **Evidence:** Verified: resolve_tz (422), parse_message_date (432) and is_outgoing (457) have no doc comments; resolve_chat_identifier (496) has only `/// Returns (chat_identifier, contact_name, unresolved_phone).` which states the tuple shape without explaining when unresolved_phone is set. **Suggestion:** Add one-line docs to these helpers and expand resolve_chat_identifier's doc to explain when `unresolved_phone` is set.
- **[low] pub fn clap_command() has no doc comment in either CLI crate** — `crates/cli/vault-push/src/cli.rs:92`. **Evidence:** vault-push/src/cli.rs:92 and vault-pull/src/cli.rs:64 both expose pub fn clap_command() -> Command with no doc, in a pub module re-exported from lib.rs as part of the crate API consumed by dump-cli-docs and the GUI. **Suggestion:** Add a one-line doc (e.g. "The clap Command for embedding --help output into GUI docs") to both clap_command functions.
- **[low] DEFAULT_PAGE_LIMIT const lacks a doc while its sibling const has one** — `crates/cli/vault-pull/src/run.rs:22`. **Evidence:** pub const DEFAULT_PAGE_LIMIT: usize = 100; at run.rs:22 has no doc comment; DEFAULT_ASSET_DOWNLOAD_WORKERS on the next line has "Default number of parallel asset download workers.", and the const is re-exported from lib.rs (line 18). **Suggestion:** Add "Default page size for GET /v1/export/messages" to DEFAULT_PAGE_LIMIT.
- **[low] No #![warn(missing_docs)] and nearly every IPC DTO struct field undocumented** — `src-tauri/src/lib.rs:15`. **Evidence:** lib.rs:15-16 declares the pub surface with no missing_docs lint; fields of ExtractArgs (extract.rs:131-144), PushArgs (push.rs:59-71), PullArgs (pull.rs:17-24), ExtractProgressEvent (events.rs:11-17), and HomeDirInfo.path (paths.rs:7-12) all lack field docs (only HomeDirInfo.os is documented). These structs are the JSON wire contract with web/src/lib/types.ts. **Suggestion:** Add #![warn(missing_docs)] to lib.rs and document the DTO fields, which are the JSON wire contract with web/src/lib/types.ts.

---
## docs-quality — 8 findings

### medium

- **[medium] Contradictory field doc: 'Absent when present.'** — `crates/libs/ir/src/lib.rs:296`. **Evidence:** Confirmed verbatim at lib.rs:296: `/// Why bytes were not imported (`too_large`, `file_missing`). Absent when present.` — reads as a self-contradiction and does not explain when missing_reason is None. **Suggestion:** Reword to "None when the attachment was imported; set (`too_large` / `file_missing`) only when bytes were skipped."
- **[medium] Broken doc link to docs/maintainers/architecture/message-ir.md** — `crates/libs/ir/src/lib.rs:7`. **Evidence:** Confirmed: lib.rs:7 links ../../../docs/maintainers/architecture/message-ir.md which does not exist in this checkout (ls: No such file or directory); docs/src/content/docs/vault/developer/architecture/ contains common-message.md instead. **Suggestion:** Point the link at the current docs-site page (common-message) or drop it; rustdoc will warn on the unresolved relative link.

### low

- **[low] Cryptic public field name fn_attr with no doc** — `crates/libs/sbr/src/read.rs:35`. **Evidence:** Confirmed: pub fn_attr: String at read.rs:35 on MmsPart holds the XML 'fn' attribute (read.rs:129: fn_attr: get(attrs, "fn").into()); no doc comment explains it, and MmsPart's ct/cl/name fields are undocumented too. **Suggestion:** Rename to filename_attr (or content_location) and add a doc comment to MmsPart and its ct/cl/name fields.
- **[low] Stale `message-media` flag name in AttachmentMedia::media_mode doc** — `crates/core/message-vault-io-core/src/exporters.rs:262`. **Evidence:** Verified: line 262 reads `/// Matching `message-media` mode used by FormatSink.` A repo-wide grep finds no flag named `message-media` (only this doc comment); the CLI flag is `--media-mode` (cli.rs:51). FormatSink appears as plain text, not a [`FormatSink`] rustdoc link. **Suggestion:** Reword to reference the `--media-mode` flag and link FormatSink as [`message_ir_format::FormatSink`].
- **[low] `[`crate`]-style run` doc phrasing is confusing** — `crates/core/message-vault-io-core/src/pipeline.rs:52`. **Evidence:** Verified: line 52 reads `/// Result of a successful exporter [`crate`]-style `run`: human-readable log lines.` [`crate`] is special rustdoc syntax resolving to the crate root; "crate-style" does not explain the return type. **Suggestion:** Rephrase to something like "Result of a successful exporter run: human-readable log lines."
- **[low] --mode help uses invented term 'resume-safe' and hides the destructive replace** — `crates/cli/vault-push/src/cli.rs:34`. **Evidence:** Doc reads "Import mode: append (resume-safe) or replace" with no warning that replace clears the vault's existing data for the source; VaultPushConfig.mode (run.rs:96) documents replace as "clears then imports". **Suggestion:** Rewrite as "append: add to existing data (safe to re-run); replace: delete existing messages for this source, then import".
- **[low] CLI about line claims iMessage-only while the tool generates three backup sources** — `crates/vault/demo-seed/src/main.rs:11`. **Evidence:** #[command(about = "Generate iMessage demo data for Message Vault")] but lib.rs:1-4 documents "three backup sources" and generate_into writes staging/imessage, staging/sms-backup-restore, and staging/whatsapp. **Suggestion:** Change about to "Generate the demo message dataset (iMessage, SMS Backup & Restore, WhatsApp) for Message Vault".
- **[low] Module intros enumerate extract:* events incompletely — extract:issue and extract:progress omitted** — `src-tauri/src/commands/mod.rs:8`. **Evidence:** commands/mod.rs:8-10 lists `extract:log`, `extract:progress`, `extract:finished`, `extract:error` but push.rs:144-153 emits `extract:issue`, which web/src/lib/tauri.ts:179 listens for; extract.rs:3-6, headed "Progress is sent back as Tauri events", omits `extract:progress` entirely. **Suggestion:** Add `extract:issue` to the commands/mod.rs event list and `extract:progress` to the extract.rs intro list.

---
## api-design — 6 findings

### medium

- **[medium] imessage-ir-exporter CLI silently drops --media-mode and the other media flags** — `crates/exporters/imessage-ir-exporter/src/main.rs:38`. **Evidence:** Verified: main.rs:38 is `media: MediaConfig::default(),` and never reads `common.media_mode`/`media_max_fps`/`media_min_size`/`media_skip_efficient`, accepted via `#[command(flatten)] CommonCli` (cli.rs:42-43). go-sms-pro and imazing mains build `MediaConfig { mode: common.media_mode, compress }` instead. **Suggestion:** Build MediaConfig from `common.media_mode` plus `compress_options_from_cli` like the other exporters, or reject the flags when non-default.
- **[medium] push command never wires the shared cancel flag, so Cancel has no effect on an in-flight push** — `src-tauri/src/commands/push.rs:113`. **Evidence:** push.rs:84 takes `_state: tauri::State<'_, Arc<Mutex<AppState>>>` and push.rs:113 sets `cancel: None` in VaultPushConfig, unlike extract/format/pull which clone the flag. The web Import flow renders a Cancel button wired to the shared flag; VaultPushConfig.cancel is honored via check_cancel when wired. **Suggestion:** Clone the cancel flag from state and pass `cancel: Some(cancel)` in VaultPushConfig (dropping the `_` prefix on state), mirroring pull.rs:47-50.

### low

- **[low] Dead config fields look live: first_last and labels.names are parsed but never read** — `crates/vault/demo-seed/src/config.rs:55`. **Evidence:** ContactsConfig.first_last (config.rs:55, #[allow(dead_code)], comment "This value is unused") and LabelsConfig.names (config.rs:67) are ignored by personas.rs, which hard-codes name-shape sampling (personas.rs:209-211 never consults first_last) and "Family/Work/College/Inactive" labels (personas.rs:156-165); demo_seed.toml:13 and demo_seed.toml:24 still ship values users would reasonably edit. **Suggestion:** Wire first_last into sample_name_shape and read group labels from LabelsConfig.names, or delete both fields and their demo_seed.toml entries.
- **[low] Every module is pub with no re-exports, exposing internals as public API** — `crates/vault/server/src/lib.rs:3`. **Evidence:** lib.rs lines 3-30 declare all 28 modules 'pub mod' (api_tokens_api through thread_tags_api), including db, media_tools, jsonl, operation_lock, import internals; no pub use re-exports appear anywhere in lib.rs. **Suggestion:** Make internal modules pub(crate) or private and re-export only the intended surface (cli, config, server entry points).
- **[low] Test-only function declared pub in the library** — `crates/vault/server/src/thread_tags_api.rs:94`. **Evidence:** "#[cfg(test)] pub fn tags_for_conversation" at thread_tags_api.rs:93-94 — a pub symbol that only exists in test builds of the crate. **Suggestion:** Make it pub(crate) or move it inside a #[cfg(test)] mod tests block.
- **[low] Dead pub field on a public struct** — `crates/vault/server/src/import.rs:85`. **Evidence:** ImportOptions.db_path is pub with #[allow(dead_code)] at import.rs:85-86; its only readers are inside import_jsonl_files (lines 279, 286), which has no non-test callers — the CLI path uses import_jsonl_files_on_conn (import_cli.rs:140). **Suggestion:** Remove the field (and the allow) or find the caller that should read it.

---
## error-handling — 5 findings

### medium

- **[medium] 'unsafe attachment path' message is a cross-crate string contract** — `crates/libs/ir-format/src/util.rs:82`. **Evidence:** Confirmed: util.rs:82 bails with 'unsafe attachment path (contains ..): {rel}'; crates/vault/server/src/import.rs:2962 and 3023 assert err.to_string().contains("unsafe attachment path"), so a wording change breaks behavior checks in another crate. **Suggestion:** Introduce a shared error type or a pub const message for this error in ir-format so the contract is enforced at compile time, not by string matching.
- **[medium] HTTP status classified by substring-matching anyhow messages** — `crates/vault/server/src/api_tokens_api.rs:46`. **Evidence:** map_label_error (api_tokens_api.rs:46-53) returns BadRequest when msg.contains("label is required") || msg.contains("at most 120"), else Internal; the strings originate from bail! at db/api_tokens.rs:319 and 322, so a reworded message silently flips the status to Internal. **Suggestion:** Return a typed error (or status enum) from create_api_token/update_api_token_label instead of parsing message text.

### low

- **[low] String error types at the crate boundary** — `crates/libs/csv/src/utc_offset.rs:12`. **Evidence:** Confirmed: parse_utc_offset at utc_offset.rs:12 returns Result<FixedOffset, String> and DateRange::parse at date_range.rs:18 returns Result<Self, String> (finding cited :15, the struct is at :9, parse at :18), while other lib crates use anyhow — callers cannot distinguish or attach context. **Suggestion:** Switch these to anyhow::Result or a small thiserror enum for consistency with the other lib crates.
- **[low] truncate() slices at a byte offset and can panic on a UTF-8 boundary** — `crates/cli/vault-push/src/http.rs:174`. **Evidence:** format!("{}…", &s[..max]) panics with 'byte index is not a char boundary' when max splits a multi-byte char; called with server-controlled bodies, e.g. truncate(&text, 200) at line 252 and truncate(&start_text, 200) at line 435. **Suggestion:** Slice on s.floor_char_boundary(max) instead of raw s[..max] in both this copy and the verbatim duplicate at vault-pull/src/http.rs:395.
- **[low] Retry classification relies on substring-matching formatted error text** — `crates/cli/vault-push/src/http.rs:800`. **Evidence:** is_transient_error lowercases error.to_string() and greps for "invalid vault key", "413", "payload too large", "no such file", "404" to decide retryability; a rewording of any bail! changes retry behavior silently. **Suggestion:** Attach a typed flag (e.g. a Retryable enum or thiserror-style tagged error) at bail sites instead of parsing message text.

---
## structure — 5 findings

### medium

- **[medium] server.rs is 3452 lines, the largest file in the workspace, mixing router setup, auth resolution, and 25+ handlers for unrelated domains** — `crates/vault/server/src/server.rs:1`. **Evidence:** largest-files.txt confirms 3452 lines (top of workspace); the file holds auth_check, import/export handlers, contacts/conversations/thread-tags/contact-groups handlers, asset GET/PUT/upload handlers; #[cfg(test)] begins at line 3040 (~410 lines of tests); grep for section banners returns nothing. **Suggestion:** Move handler bodies into the existing per-domain modules (contacts_api, conversations_api, assets) and keep server.rs for router assembly and shared auth helpers.
- **[medium] import.rs is a 3039-line monolith with no module doc and no section markers** — `crates/vault/server/src/import.rs:1`. **Evidence:** largest-files.txt confirms 3039 lines; functions span staging (import_file_to_staging:879), promote (promote_append:1235, promote_messages_chunked:1560), contacts (apply_contact_name_mode:738, seed_contact_handle_alias:692), media (try_store_converted:506); grep for section banners or //! intro returns nothing. **Suggestion:** Split import.rs into staging, promote, and contacts/name submodules (or at minimum add section banner comments and a //! intro).

### low

- **[low] mms_enc.rs is a 2021-line monolith** — `crates/libs/go-sms-mms/src/mms_enc.rs:203`. **Evidence:** Confirmed: file is 2021 lines; StructuredMms impl merging starts ~203, decode_uint_var at 383, decode_message_type_value at 688 — roughly 30 unit decoders in one file; it is the largest lib file (ranks 5th in crates/ overall). **Suggestion:** Split the decode_* unit decoders into a private submodule and keep mms_enc.rs for the PDU assembly logic.
- **[low] Four exporter emit.rs files exceed 1000 lines** — `crates/exporters/imazing-exporter/src/emit.rs:1`. **Evidence:** Verified with wc -l: imazing/emit.rs 1225 lines, imessage-ir/emit.rs 1197, go-sms-pro/emit.rs 1162, sms-backup-plus/emit.rs 1010. Each file mixes parsing helpers, document building (pending_to_document) and attachment handling. **Suggestion:** Split document building and attachment handling into separate modules per exporter, then share the common parts.
- **[low] extract.rs (626 lines, largest file in the crate) mixes command, config building, JSONL counting, and a log-progress parser** — `src-tauri/src/commands/extract.rs:449`. **Evidence:** extract.rs:449-561 holds six log-parsing helpers (extract_progress_from_log, is_writing_conversation_files_banner, extract_progress_ratio, trailing_usize, leading_usize) plus the async command (159-252), build_exporter_config (313-415), count_jsonl_output (86-126), and tests; at 626 lines it is the largest file in the crate (next: push.rs at 265). **Suggestion:** Move the log-line progress parser and its tests into a dedicated module (e.g. commands/progress.rs), leaving extract.rs as the command and config-building surface.

---
## best-practices — 3 findings

### medium

- **[medium] No #![warn(missing_docs)] anywhere in the lib crates** — `crates/libs/ir/src/lib.rs:1`. **Evidence:** grep -rn 'missing_docs' across crates/ returns no matches (exit 1); every lib crate starts directly with a //! intro and no lint gate, so undocumented pub items go unnoticed. **Suggestion:** Add #![warn(missing_docs)] (or a workspace [lints] table) to each lib crate and fix the resulting warnings.
- **[medium] src-tauri is excluded from the workspace, so CI never builds, tests, or lints it on PRs** — `src-tauri/Cargo.toml:15`. **Evidence:** Root Cargo.toml:30 `exclude = ["src-tauri"]` and src-tauri/Cargo.toml:12 declares its own `[workspace]`. ci.yml runs only `cargo build --workspace` / `cargo test --workspace`; the Tauri build runs only in the tag-gated release job, and no clippy step exists in any workflow. **Suggestion:** Add a PR job running cargo clippy and cargo test with --manifest-path src-tauri/Cargo.toml, and consider joining src-tauri into the main workspace.

### low

- **[low] sha2 dependency is unused and pins a different major version than vault-push** — `crates/cli/vault-pull/Cargo.toml:27`. **Evidence:** sha2 = "0.10" is declared but no source file imports sha2 (grep for 'use sha2' and 'sha2::' in vault-pull/src returns nothing); vault-push uses sha2 0.11.0, so the workspace compiles two sha2 majors and pulls one needlessly. **Suggestion:** Remove sha2 from vault-pull/Cargo.toml dependencies.

---
## http-docs — 2 findings

### high

- **[high] Route-echo summaries with '# Errors' rustdoc sections leaking into OpenAPI descriptions** — `crates/vault/server/src/api_tokens_api.rs:112`. **Evidence:** Doc comments at lines 112, 149, 210, 252 echo the route ('`GET /v1/account/api-tokens`') followed by '# Errors' + 'Returns an API error when the caller is not a signed-in session...'; utoipa-summaries.txt lines 14-17 flag all four with ECHOED-ROUTE-SUMMARY ERRORS-SECTION-IN-DESCRIPTION. **Suggestion:** Rewrite each handler doc as plain prose (e.g. "List the account's named API tokens with scopes and masked secrets") with no route echo and no '# Errors' heading so the OpenAPI summary/description stop carrying rustdoc boilerplate.
- **[high] ~30 endpoints have no OpenAPI summary or description** — `crates/vault/server/src/server.rs:981`. **Evidence:** Handlers contacts_list_handler (981), conversations_list_handler (1018), conversation_sources_handler (1051), contact_mutate_handler (1151), contact_groups_list_handler/members/membership (1267-1425), auth_check (691), import_handler (2045), export_messages_handler (2387), asset_put_handler (2437) each have #[utoipa::path] immediately preceded by no doc comment; utoipa-summaries.txt flags 32 routes with NO-SUMMARY NO-DESCRIPTION matching these handlers. **Suggestion:** Add a one-line summary doc comment (or summary=/description= inside #[utoipa::path]) to every handler listed in openapi.rs api_openapi(), starting with the export/contacts/import endpoints.

---
## unsafe — 2 findings

### medium

- **[medium] SAFETY comments on env::set_var/remove_var justify variable ownership, not the std-required no-concurrent-env-access condition** — `src-tauri/src/commands/ffmpeg.rs:62`. **Evidence:** ffmpeg.rs:62-64: "SAFETY: this desktop process owns MESSAGE_VAULT_IO_BIN for the session." In edition 2024 set_var/remove_var are unsafe because any other thread reading or writing the environment concurrently is UB; the comment justifies per-variable ownership, not that precondition. **Suggestion:** Rewrite both comments to state the actual precondition (no other thread accesses the environment concurrently) and serialize env mutation or route it through one dedicated thread.

### low

- **[low] unsafe libc::flock call with no SAFETY comment duplicates the fs2 dependency already used elsewhere** — `crates/vault/server/src/asset_uploads.rs:164`. **Evidence:** Line 164: 'let rc = unsafe { libc::flock(file.as_raw_fd(), libc::LOCK_EX) };' inside lock_session — no SAFETY comment; operation_lock.rs:13 imports fs2::FileExt and uses it for the same exclusive-lock job. **Suggestion:** Replace with fs2::FileExt::try_lock_exclusive (as operation_lock.rs does) or add a SAFETY comment justifying the raw flock.

---
## Follow-up proposals

| Name | Crates | Rationale | Findings |
|---|---|---|---|
| Libs: missing_docs gate and full public-surface documentation | message-ir, message-ir-format, sbr, media, contacts, message-csv, go-sms-mms, obfuscate | Add #![warn(missing_docs)] to every lib crate and document the full public surface (the IR model every exporter writes, re-exported reader/probe types, public fields), fixing the broken link, contradictory field docs, and triplicated attachment metadata along the way. | 13 |
| Exporters: shared pipeline extraction plus config/CLI docs | All 7 exporter crates and message-vault-io-core | Hoist the duplicated run()/main()/bump()/attachment-naming/mime-table/col()/sha256/convert_smoke helpers into message-vault-io-core and libs/media, document ExporterConfig and Form, and fix the silently dropped --media-mode in imessage-ir. | 19 |
| CLI tools: shared vault client and journal, honest help text | vault-push, vault-pull, dump-cli-docs, demo-seed | Extract the duplicated JSONL journal and HTTP client (fixing truncate's UTF-8 boundary panic and substring retry classification), drop the unused sha2, and document the pub API surface while rewording the misleading --mode, about, and clap_command docs. | 12 |
| Server: OpenAPI prose, module decomposition, typed errors | message-vault-server | Replace route-echo and '# Errors' handler docs with plain-prose summaries and descriptions for ~30 endpoints and ToSchema structs, add module intros and the missing_docs gate, split server.rs and import.rs, and replace substring-classified HTTP statuses and raw flock with typed equivalents. | 14 |
| Tauri host: cancel wiring, sound SAFETY comments, DTO docs, CI | message-vault-io-tauri (src-tauri) | Wire the shared cancel flag into push, rewrite the env::set_var SAFETY comments to state the real concurrency precondition, document the IPC DTO wire contract, and add a clippy/test CI job for the workspace-excluded crate. | 7 |

## Coverage gaps

- crates/message-vault-io-gui: 0 findings by design (excluded from scope) — 4112 lines across 12 files (sync.rs 559), Slint ui/ tree, build.rs, anyhow pinned to 1.0.103, depends on all 8 exporters; next pass needs docs-coverage, error-handling, api-design on its pub surface, structure
- best-practices 'duplicate dependencies' had no mechanical signal: no cargo tree -d / cargo machete / udeps evidence file, yet root Cargo.lock has 70 packages at 2+ versions (thiserror 1.0.69+2.0.19, base64 0.21+0.22, hyper 0.14+1.11, tower-http 0.6+0.7, syn 1/2/3, toml 0.8+1.1) — claim unverified
- src-tauri is a [workspace] orphan with a tracked second lockfile (src-tauri/Cargo.lock) and own dep versions (dirs="6", anyhow "1.0", serde "1") outside workspace.dependencies; no CI check keeps it in sync with the root lock, and cargo test --workspace never compiles it
- docs-coverage heuristic excludes lib.rs and main.rs, so //! crate intros, crate-level docs, and #![warn(missing_docs)] had no evidence; verified ZERO of 20 lib.rs files gate missing_docs — the 'no missing_docs enforcement' claim rests only on hand inspection
- docs-quality 'broken intra-doc links' unverified mechanically: no cargo doc -D rustdoc::broken_intra_doc_links run, and CI has no docs job at all
- unsafe dimension is mostly false positives: 13 grep hits but 6 are the substring in bail!/assert/doc strings (vault-push/run.rs:732,1700; server/config.rs:137; ir-format/util.rs:82; import.rs:2962,3023); real surface is ~7 sites, incl. std::env::set_var in src-tauri/src/commands/ffmpeg.rs:79 inside a multi-threaded Tauri runtime — needs an explicit soundness+safety-comment verdict; also src-tauri/build.rs was outside the src-tauri/src scan path
- error-handling 'thiserror consistency' has an unexamined counterexample: both thiserror 1.0.69 and 2.0.19 are in the lock, and gui pins anyhow = "1.0.103" exactly — who is on old thiserror and why was never asked
- cross-crate duplication pass centered on exporters (attachments.rs, emit.rs) but the triplicated exporter-invocation wrapper — vault/server/src/export_api.rs vs src-tauri/src/commands/{extract,format,events,push,pull}.rs vs gui/src/sync.rs — and the 1251-line crates/cli/vault-push/tests/push_mock.rs test scaffolding pattern are not evidenced as swept
- structure dimension is file-level only (largest-files.txt): no function-length signal inside vault/server/src/server.rs (3452), import.rs (3039), vault-push/src/run.rs (2880), go-sms-mms mms_enc.rs (2021) — oversized functions inside them unchecked
- CI gap confirmed but only half claimed: ci.yml has no clippy job (fmt, test, web lint/test, docker, desktop, release only) and no missing_docs/rustdoc job — the rustdoc half belongs to no dimension's evidence

The audit's evidence is genuinely uneven: server, libs, duplication, and http-docs are well supported (utoipa-summaries.txt 62 lines, 328-line missing-docs heuristic for the module/function level, largest-files histogram), so the thinnest dimensions are the ones with no evidence file at all — best-practices dependencies (70 duplicate-version packages in root Cargo.lock, unexamined) and docs-coverage at the crate level (lib.rs excluded from the heuristic; zero #![warn(missing_docs)] anywhere, so the style guide can't cite any enforced crate). Crate-wise the hole is not src-tauri (7 findings is proportionate to its small src/ surface) but the deliberately excluded message-vault-io-gui — a whole 4k-line workspace member with its own build.rs, Slint UI, and 8-exporter dependency fan that no dimension touched; a follow-up pass should audit it plus the gui/src-tauri/server exporter-wrapper triad, run cargo tree -d / machete on both lockfiles, and add a cargo doc + clippy job to CI so the un-verified claims (intra-doc links, missing_docs enforcement, clippy clean) become mechanically checkable. One process caveat: the scratch evidence under .tmp/rust-audit/ is gitignored and ephemeral — the evidence summaries (unsafe-site list, lockfile dup counts, heuristic coverage limits) are embedded in this report instead, so the 65 verified findings stay reproducible.

## Verification

- All 65 findings in this report passed adversarial verification (claim, severity, not-already-fixed). The workflow started with 67 candidate findings; 2 were dropped during verification.
- Re-checked at the cited lines: 3 findings — all three high-severity findings, the full set since no high-severity duplication findings existed. Re-check log: 3 PASS, 0 FAIL (`.tmp/rust-audit/recheck.log`). No findings were removed by re-check.

## Out of scope

Legacy Slint GUI (`crates/message-vault-io-gui`), `web/`, `web-next/`, Starlight prose pages, dependency bumps.
