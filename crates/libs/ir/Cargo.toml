[package]
name = "message-ir"
version = "0.1.0"
edition = "2024"
description = "Schema types for the common-message ConversationDocument intermediate representation"
license = "LicenseRef-FCL-1.0-ALv2"

[dependencies]
message-csv = { path = "../csv" }
serde = { version = "1.0.228", features = ["derive"] }
serde_json = "1.0.150"
