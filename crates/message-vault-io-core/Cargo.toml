[package]
name = "message-vault-io-core"
version = "0.1.0"
edition = "2024"
description = "Shared form models and process spawning for message-vault-io GUIs"

[dependencies]
message-csv = { path = "../message/csv" }
media = { path = "../message/media" }
rust-ini = "0.21"

[dev-dependencies]
tempfile = "3.27.0"
