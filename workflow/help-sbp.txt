warning: function `write_export_sentinel` is never used
  --> crates/message/ir-format/src/clean.rs:15:8
   |
15 | pub fn write_export_sentinel(output_dir: &Path) -> Result<()> {
   |        ^^^^^^^^^^^^^^^^^^^^^
   |
   = note: `#[warn(dead_code)]` (part of `#[warn(unused)]`) on by default

Convert EML tree via common message to the chosen packaging format

Usage: sms-backup-plus-exporter convert [OPTIONS] --output <OUTPUT>

Options:
      --input <INPUT>
          Path to a .eml file or directory tree of EMLs (Archive/, Sent/, …). Repeat for multiple roots; trees are merged and path-deduped. Default: source_dirs from config/owner.toml when set
  -v, --verbose
          Log progress to stderr (inputs, scan/write progress, dedupe summary)
      --no-summary
          Skip the end-of-run summary on stdout
      --owner-phone <OWNER_PHONES>
          Owner phone (E.164 or digits). Repeat for multiple owner numbers. Default: `phones` in config/owner.toml
      --owner-email <EMAIL>
          Owner email addresses used to detect sent messages when X-smssync-type is missing. Default: `emails` in config/owner.toml
      --name-mapping <NAME_MAPPING>
          Name mapping CSV (`Phone,Incorrect Name`) for EML export aliases. Default: config/name-mapping.csv when that file exists
      --output <OUTPUT>
          Output directory for packaging + attachments/
      --format <FORMAT>
          Output format: `json` (default), `jsonl`, `csv`, `eml`, `mbox`, or `xml` [default: json]
      --contacts <CONTACTS>
          vCard CSV or VCF contacts file for phone→name resolution. Optional; without it phone numbers are not resolved to names
      --vcf <VCF>
          Contacts VCF (alternate to `--contacts`)
      --obfuscate
          Rewrite output with stable, non-reversible fake names/numbers/text and placeholder media
      --obfuscate-seed <OBFUSCATE_SEED>
          Optional seed for reproducible obfuscation (implies --obfuscate)
      --start-date <YYYY-MM-DD>
          Only messages on or after this date (YYYY-MM-DD, local midnight, inclusive)
      --end-date <YYYY-MM-DD>
          Only messages before this date (YYYY-MM-DD, local midnight, exclusive)
      --media-mode <MODE>
          Attachment media: disabled (no files), clone (default), convert, or compress [default: clone]
      --media-max-resolution <RES>
          Compress only: max long edge (720p, 1080p, 4k) [default: 1080p]
      --media-max-fps <MEDIA_MAX_FPS>
          Compress only: max frame rate [default: 30]
      --media-min-size <MEDIA_MIN_SIZE>
          Compress only: only re-encode videos at/above this size (e.g. 20M) [default: 20M]
      --media-skip-efficient <MEDIA_SKIP_EFFICIENT>
          Compress only: skip already-efficient HEVC under max resolution (default on) [default: true] [possible values: true, false]
  -h, --help
          Print help
